{% extends 'base.html.twig' %}

{% block title %}Edit Produit
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('styles/produit/edit.css') }}">
{% endblock %}

{% block body %}
	<h1>Modifier le produit</h1>

	{% if produit.image %}
		<p>Image actuelle :</p>
		<img src="{{ asset('uploads/images/' ~ produit.image) }}" alt="Image actuelle" width="200">
	{% endif %}

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">Mettre à jour</button>
	{{ form_end(form) }}


	{{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_produit_index') }}">back to list</a>

	{{ include('produit/_delete_form.html.twig') }}
{% endblock %}
